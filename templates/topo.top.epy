; Include forcefield parameters
#include "ffG53a6.itp"

; Include chain topologies
#include "{{substrate["itp_file"]}}"
{%- for res in resMixture.values() %}
{{'#include "{0}"'.format(res["itp_file"]) if res["count"] > 0}}
{%- endfor %}

[ system ]
; Name
System

[ molecules ]
; Compound        #mols
{%- for cluster in resnameClusters %}
{{ cluster }}
{%- endfor %}
